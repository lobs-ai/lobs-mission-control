Fixed: clicking off a node in WorkflowGraphView now closes node details. Added .allowsHitTesting(false) to decorative/non-interactive views (StageLaneBackground, EdgeLine, StageHeaderView, LegendView) so taps pass through to the background clear Rectangle which fires selectedNode = nil. Build passes; swift test pre-existing XCTest module failure unrelated to this change.
