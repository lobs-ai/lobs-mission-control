BLOCKED: Issue requires server-side fix. Agent statuses are polled from `/api/agents` every 30s, but if tasks complete faster than the polling interval, the "working" status is never captured client-side. The server needs to either: (1) update agent status more frequently, (2) keep status="working" for longer, or (3) add a recentActivity field. Client can only display what server provides.