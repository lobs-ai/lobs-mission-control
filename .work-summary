Fixed AddTaskSheet validation to check actual target project ID instead of just picker visibility. Now correctly validates project regardless of whether picker is shown, preventing disabled button when creating from project overview. Build successful (0.13s). Created AddTaskSheetValidationTests.swift with 25 comprehensive tests.