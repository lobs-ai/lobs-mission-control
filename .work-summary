Fixed project pre-selection when adding task from project card. Added vm.showOverview = false to onAddTask closure, matching onSelect behavior. Now sheet receives correct projectId instead of nil. Build successful (3.50s). Created AddTaskProjectPreselectionTests.swift with 17 tests covering state transitions, user flows, and edge cases.