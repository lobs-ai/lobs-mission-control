Fixed calendar week/month view server error by adding missing fields to ScheduledEvent model (fireCount, lastFiredAt, nextFireAt, recurrenceEnd, task fields). Server was returning these fields causing JSON decoding to fail. Build successful (0.13s). Created CalendarRangeDecodingTests.swift with 14 comprehensive tests.