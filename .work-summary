Fixed workflow connections not showing: WorkflowDetailView now fetches the full workflow definition via fetchWorkflow(id:) on appear, since the list endpoint (/api/workflows) omits nodes/edges arrays. Graph uses fullWorkflow when available, falling back to the list-response object. Build passes; XCTest module unavailable in this environment (pre-existing).
