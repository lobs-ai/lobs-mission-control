Updated workflow graph nodes to visibly show connection counts and linked node labels directly in each node card, and expanded card sizing for readability. Added tests for new NodeChip connection summary/token formatting helpers. `swift build` passes; `swift test` fails in this environment with `no such module 'XCTest'`.